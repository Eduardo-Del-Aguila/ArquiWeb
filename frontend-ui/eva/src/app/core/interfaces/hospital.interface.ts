export interface HospitalShow {
  name: string;
  direction: string;
  phone: string;
  city: string;
}

export interface HospitalInsert {
  name: string;
  direction: string;
  phone: string;
  city: string;
}